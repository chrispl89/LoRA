# FastAPI
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.31.0
python-multipart==0.0.6

# Database
sqlalchemy>=2.0.36,<2.1.0
alembic>=1.14.0,<2.0.0
# NOTE: psycopg2-binary does not ship wheels for some newer Python versions (e.g. 3.13).
# Use psycopg v3 binary wheels instead.
psycopg[binary]==3.2.3

# Validation
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
email-validator==2.1.0

# Celery
celery==5.3.4
redis==5.0.1

# S3 / MinIO
boto3==1.29.7
minio==7.2.0

# Image processing
Pillow==11.1.0
imagehash==4.3.1

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Logging
structlog==23.2.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Utilities
python-dotenv==1.0.0
